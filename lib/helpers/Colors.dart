import 'package:flutter/material.dart';

class AppColors {
  Color bgColor = Colors.white;
  Color primaryColor = const Color.fromARGB(255, 76, 2, 173);
  Color txtWhite = const Color.fromARGB(
    204,
    255,
    255,
    255,
  );
}
